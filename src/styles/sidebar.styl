@import 'swatches'

.sidebar {
  ul {
    list-style: square;
    margin: 40px;
    padding: 0;
    text-indent: 0;

    li {
      margin: 10px;
    }

    a {
      color: $tertiary;
      text-decoration: none;
      display: block;
    }
  }
}
