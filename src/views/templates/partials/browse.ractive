<ul>
  {{#if documents}}
    {{#each documents}}
      <li>
        {{#.rows}}
          {{.doc.title}}
        {{/.rows}}
      </li>
    {{/each}}
  {{else}}
    <li>No documents yet. <a href='#new'>Add one?</a></li>
  {{/if}}
</ul>
