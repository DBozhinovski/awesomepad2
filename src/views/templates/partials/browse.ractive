<ul>
  {{#if documents}}
    {{#each documents}}
      <li>
        <a href='#/show/{{id}}'>{{.title}}</a>
      </li>
    {{/each}}
  {{else}}
    <li>No documents yet. <a href='#new'>Add one?</a></li>
  {{/if}}
</ul>
